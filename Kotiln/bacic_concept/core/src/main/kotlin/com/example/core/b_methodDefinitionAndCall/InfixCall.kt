package com.example.core.b_methodDefinitionAndCall

fun main() {
    val mapInfix = mapOf(1 to "one", 2 to "two", 4 to "four", 5 to "five")
    val map = mapOf(1.to("one"), 2.to("two"), 4.to("four"))

    println("mapInfix = $mapInfix")
    println("map = $map")

}