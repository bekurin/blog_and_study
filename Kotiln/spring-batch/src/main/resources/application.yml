spring:
  batch:
    job:
      name: interceptingJob
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/spring-batch
      username: root
      password:
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true